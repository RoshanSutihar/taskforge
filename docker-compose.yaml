version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    # Remove this line if present:
    # container_name: taskforge-postgres
    environment:
      POSTGRES_USER: taskforge
      POSTGRES_PASSWORD: taskforge
      POSTGRES_DB: taskforge
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./migrations:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U taskforge"]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      replicas: 1

  redis:
    image: redis:7-alpine
    # Remove this line if present:
    # container_name: taskforge-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      replicas: 1

  orchestrator:
    build:
      context: .
      dockerfile: Dockerfile.orchestrator
    # Remove this line if present:
    # container_name: taskforge-orchestrator
    environment:
      DATABASE_URL: postgres://taskforge:taskforge@postgres:5432/taskforge?sslmode=disable
      REDIS_URL: redis://redis:6379/0
      ORCHESTRATOR_PORT: :8080
      HEARTBEAT_TIMEOUT: 15
      REAPER_INTERVAL: 10
      SCHEDULER_INTERVAL: 1
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      replicas: 1

  worker:
    build:
      context: .
      dockerfile: Dockerfile.worker
    environment:
      DATABASE_URL: postgres://taskforge:taskforge@postgres:5432/taskforge?sslmode=disable
      REDIS_URL: redis://redis:6379/0
      WORKER_CONCURRENCY: 10
      HEARTBEAT_INTERVAL: 5
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      orchestrator:
        condition: service_started
    restart: unless-stopped
    deploy:
      replicas: 3  # ← This now works without container_name

volumes:
  postgres_data:
  redis_data: